# FCC public surface — NOT YET BUILT

This directory is an inert placeholder required by the frozen repository
architecture (§1). No FCC pages, UI, or public-facing build exist yet.
Site construction is a separately gated action per FCC Stage 0
Implementation Architecture v0.1.2, build-sequence step 14.
